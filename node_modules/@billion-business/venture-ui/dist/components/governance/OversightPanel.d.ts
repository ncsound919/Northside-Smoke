import * as react_jsx_runtime from 'react/jsx-runtime';

type OversightMode = "shadow" | "checkpoint" | "recovery";
interface EscalationEvent {
    id: string;
    agent: string;
    action: string;
    reason: string;
    confidence: number;
    timestamp: string;
    mode: OversightMode;
    status: "pending" | "approved" | "rejected" | "escalated";
    risk: "low" | "medium" | "high" | "critical";
}
interface OversightPanelProps {
    events?: EscalationEvent[];
    onApprove?: (id: string) => void;
    onReject?: (id: string) => void;
    mode?: OversightMode;
    onModeChange?: (mode: OversightMode) => void;
}
declare function OversightPanel({ events: initialEvents, onApprove, onReject, mode, onModeChange, }: OversightPanelProps): react_jsx_runtime.JSX.Element;

export { type OversightMode, OversightPanel };
