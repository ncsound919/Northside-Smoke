import * as react_jsx_runtime from 'react/jsx-runtime';

interface ReasoningResult {
    summary: string;
    threats: string[];
    recommendations: string[];
    threatLevel: number;
    reasoning: string;
}
interface BrainInterfaceProps {
    onSend?: (message: string) => Promise<ReasoningResult>;
    initialMessage?: string;
    mode?: "security" | "general" | "oversight";
}
declare function BrainInterface({ onSend, initialMessage, mode }: BrainInterfaceProps): react_jsx_runtime.JSX.Element;

export { BrainInterface };
