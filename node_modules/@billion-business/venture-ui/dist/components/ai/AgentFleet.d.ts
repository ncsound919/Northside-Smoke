import * as react_jsx_runtime from 'react/jsx-runtime';

interface AgentWorker {
    id: string;
    name: string;
    task: string;
    status: "idle" | "working" | "complete" | "error" | "awaiting_approval";
    load: number;
    confidence?: number;
}
interface AgentFleetProps {
    workers?: AgentWorker[];
    onSpawnWorker?: () => void;
    onViewNodeGraph?: () => void;
}
declare function AgentFleet({ workers: initialWorkers, onSpawnWorker, onViewNodeGraph }: AgentFleetProps): react_jsx_runtime.JSX.Element;

export { AgentFleet };
